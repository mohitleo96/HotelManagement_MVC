@model IEnumerable<HotelManagement.Models.DEPARTMENT>

@{
    ViewBag.Title = "Index";
}

<font style="font-family:'Times New Roman', Times, serif; color:antiquewhite; font-size:larger">
    <b>  <h2>DEPARTMENT</h2></b>


    <p>
        @Html.ActionLink("ADD_DEPARTMENT", "Create")
    </p>
    <table class="table" border="2">
        <tr style="background-color:cornsilk">
            <th>
                @Html.DisplayNameFor(Model => Model.Department_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department_Name)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Department_ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Department_Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Department_ID }) |

                    @Html.ActionLink("Delete", "Delete", new { id = item.Department_ID })
                </td>
            </tr>
        }

    </table>
    @Html.ActionLink("Back", "IndexOwner", "DASHBOARD")
